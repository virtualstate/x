import {BrowserRuntimeOptions} from "./runtime.browser.options";
import {h, VNode} from "@virtualstate/fringe";

export async function Runtime({ document }: BrowserRuntimeOptions, input?: VNode) {

}
