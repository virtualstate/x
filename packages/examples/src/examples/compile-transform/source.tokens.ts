import {createToken} from "@virtualstate/fringe";

export const View = createToken(Symbol("View"));
export const Text = createToken(Symbol("Text"));

export const Source = createToken(Symbol("Source"));
