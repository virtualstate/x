import { Context, State } from "./source.interface";
import { VNode } from "@virtualstate/fringe";

export interface TransformOptions {
  url: string
}

export async function *Transform(options: TransformOptions, state?: VNode) {

}
