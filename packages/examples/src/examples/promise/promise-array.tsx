import { h } from "../../jsx";

export const _302_PromiseArray = (
  <container>
    {[
      Promise.resolve("🎾"),
      Promise.resolve("🧩")
    ]}
  </container>
)
export const _302_URL = import.meta.url;
